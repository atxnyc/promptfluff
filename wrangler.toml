# Cloudflare Workers (Static Assets) config for the promptfluff marketing site.
# Serves the site/ folder as static assets — no Worker script, no build step.
# The connected git build runs `npx wrangler deploy`, which uploads ./site.
name = "promptfluff"
compatibility_date = "2026-06-13"

[assets]
directory = "./site"
